# Used only by the /api/weather serverless function (Vercel deployments, or
# `vercel dev` locally). Standard local development via config.js does not
# need this file — see README.md.
WEATHER_API_KEY=YOUR_WEATHERAPI_KEY
